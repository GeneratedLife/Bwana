rootProject.name = 'bwana'

// core holds the toolkit and knows nothing about any client. client-225 is the
// Lost City rev 225 client with the adapter that implements core's interfaces.
// A second revision is a second client-* module beside this one; core is shared
// rather than copied, which is the whole point of the arrangement.
include 'core'
include 'client-225'
